name: Testing github context
run-name: ${{ github.actor }} 
on:
    push
jobs:
    context:
        runs-on: ubuntu-latest
        steps:
            - name: context
              run: ${{toJson(github)}}